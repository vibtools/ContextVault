[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "contextvault"
version = "1.0.0"
description = "ContextVault"
requires-python = ">=3.12"